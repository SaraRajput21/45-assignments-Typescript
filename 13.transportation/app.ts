// Define an array to store examples of favorite modes of transportation
const favoriteTransportation: string[] = ["motorcycle", "car", "bicycle", "scooter", "pickup truck"];

// Loop through the array and print statements about each item
for (let i = 0; i < favoriteTransportation.length; i++) {
    console.log(`I would like to own a ${favoriteTransportation[i]}.`);
}

